v0.5.9
- Fix Chemical Plant recipe output for fluids.
- Fix for Storage-only electrical network balancing.
- Increased Nails&Strips Metal Press output to realign with vanilla smithing plate output.
- Add missing Blast Furnace localization strings for HUD output window.
- Save and Load Motor/Alternator Machine State

v0.5.8
- Add Chemical Plant
- Fix fridge once again
- Add plastic pellets (for future crafting recipes)
- Finish MB block color standarization for commonly used types.
- Refactor LV Charger, introduce ItemChargable base-class for items, otherwise use IChargableItem interface for custom items.

v0.5.7
- Fix recipe conflict for Steel Machine Block/MB Component, fixes #90
- Removed the Exception thrown if a machine entity is null when a network is initialized. Prints an error on the screen and in the log every 2 seconds with the block position of the null entity.
-- To help track down this issue, find the position, report what machine is there, break the machine and replace/rewire it. The more data I have to work with the better chance I can find why its happening.

v0.5.6
- Bug fix, Electric Networks are marked Dirty on world load and initialized when ticking starts, ensuring the world is actually loaded.
- Added crafting process to make Sheet Metal blocks.
v0.5.5
- Added Distillation Tower Base and Extension, up to 6 extensions can be built on top of the base.
- Added Fluid Generator, an MV generator, power output depends on fuel used.
- Tested Derrick, Pumpjack, LV Pump, Fluid Generator, Distillation Tower; Fixing several issues.
- Refactored Electric Network subsystem to use events and delegates.
- Added liquid fuels: diesel, heavy oil, lubricant, kerosene, gasoline, naptha, and LPG
- Fixed #80, #82, and #83

v0.5.2
- Added Derrick Multiblock
- Added Well Casing for the Derrick
- Added PumpJack Multiblock
- Added Steel & Iron Fence
- Added Steel Ladders
- Added Metal Platform Top variant
- Added Treated Wood slabs, recipes, and crafting chains
- Renamed Metal Rod to Metal Bar, all your VintEng Rods should now be bars. Vanilla added Rods so I adapted.
- Added new Rod Die Cast for extruder to make vanilla Rods. These are 1:1 with ingots.
- LV Pump now only pumps fluid at or above the level the basket is in.
- Added a Fridge
- Combined Scaffold types into one code with variants. Freed up "Girder" for something better. <_<
- Various Shenanigans...
- Fixed #49, #59, #60, #64

v0.5.0
- Fix Tank-Bucket interaction when holding stacks of buckets.
- Reduce by a further 50% the spawn chance of Oil Geysers.
- Add Creosote Oven as a Multiblock (MB) that you have to build block-by-block
- Fix Pipe Filter bug #56
- Add all MB component pieces and recipes. Recipes not final, balance suggestions welcome.

v0.4.9
- Added new component for IBlockEntityContainer interface that was added in 1.21.6.
- This seemed to have cause all sorts of issues. Please devs, no major API changes for sub-version releases. This really threw me.

v0.4.8
- Multiblock progress, nothing to use yet, but progress is progress...
- New Block types added for Multiblock (MB) construction:
  - Treated Wood variants
  - IO Blocks to indicate MB interaction points (Item, Fluid, Power, etc.)
  - Metal Component Blocks (Light/Heavy Engineering blocks and Steel Reinforced Block)
  - Interaction IO Block type is a special block for MB validation and construction and should not be used as a Deco block.
- Handbook crash Bug Fixed, fixes #43
- Fixed #46, forge heating ingots properly.


v0.4.5
- New MV Networks! MV Batteries and LV-MV Transformers
- Major Bugfixes regarding Pipes, pipe networks, and chunk boundaries
------------------------------------------------DEBUG-------------------------------------------------------------
- New Debug commands to validate electric network systems:
  The [] show an optional flag, use delete with extreme caution. An example is /vedebug vpn delete
  You will need full server access privileges to use any of these.
  - /vedebug validatepowernetworks [delete]
  - /vedebug vpn [delete]
    - Validate all electric networks

  - /vedebug validate id [delete]
    - Validate a single ID with optional delete

 - New Debug commands to validate pipe networks
  - /vedebug validatepipenetworks [delete]
  - /vedebug vpipes [delete]
    - Validate all pipe networks

  - /vedebug validatepipes id [delete]
    - Validate a single give pipe network ID

 - New Debug commands to debug Catenary Wire data
  - /vedebug validatewires [delete]
  - /vedebug vw [delete]
    - Validates all Wire Connection objects in Catenary Data, ensures all wire endpoints block types are WiredBlock. Optionally Delete invalid nodes, will not drop wire or fire event.

  - /vedebug numwires
    - Prints total number of Wire Connections in the data set.
  - /vedebug printwires
    - Prints to the VerboseDebug log every WireConnection in the world as stored by the CatenaryMod. Validate first. Use to track down and report odd wires in your world.

  A network is only reported as invalid if ALL nodes are invalid, if one node (aka position) comes back as valid it will report as having valid nodes.
  To ensure a full game-save cleanup, break down the entire factory: pipes, machines, wires, and relays before running the validate command for each type of network.
  That way, any full network that is truly invalid can be deleted using the optional flag.
-------------------------------------------------------------------------------------------------------------------
 - new Electric ModConfig file that allows players/server owners to customize PPS limits for each tier of network.
  *(note, this network PPS feature has not yet been implemented, but will be soon! This is added now to prep for that feature.)*
 - Removed many MarkDirty events to help streamline Client-Server data transfer.
 - Added special case to Kiln to make QuickLime craftable in the machine, go wild!
 - Added Null checks to Recipe Handbook function to (hopefully) fix #43
 - If any output slot is full, Sawmill should now stop processing
 - Rebalanced slightly Board output of Sawmill log processing recipe. 16->14
 - Wires will no longer render during Temporal Storms to save on GPU load. Feedback appreciated.
 - Simple blocks like the Battery and Transformer should never go to Sleep, these entities do not tick so sleeping is not needed.
 - Change Item Pipe ItemMoveOperation to default to DirectMerge instead of AutoMerge, should behave as if the player forced the items to stack. Can revert if this causes issues.
 - Fixed: Crash when an extraction pipe is in RoundRobin mode and a new inventory block is placed that connects to the pipe network. (Enumerator error)
 - (Starting to) Standardize MarkDirty calls for all machine BlockEntities. Should have done this long ago... Default update rate is twice per second.
 - Newly placed machines should default to On, then go to sleep if required.